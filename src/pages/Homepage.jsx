import React from 'react';
import Menu from '../component/Menu';

const Homepage = () => {
    return (
        <div>
            <Menu/>
            <h1>Homepage</h1>
        </div>
    );
};

export default Homepage;